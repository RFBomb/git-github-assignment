Instructions:
For this week's assignment, you will write your own stack using templated class in C++.
The basic outline of the class should be:
template <typename T>

class Stack {

    private: std::vector<T> elements;

}
You need to add the methods for each of the following:

    push()
    pop()
    isEmpty()

Use the class in an example application.
Requirements:
Submit a Word document with all of your code.
There are several C++ online compilers if you don’t want to install Visual Studio such as Programiz - Online C++ Compiler