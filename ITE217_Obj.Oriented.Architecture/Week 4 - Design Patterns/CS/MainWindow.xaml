<Window x:Class="ITE217_Week4_SingletonPattern.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ITE217_Week4_SingletonPattern"

        xmlns:FF="clr-namespace:MvvmDialogs;assembly=MvvmDialogs"
        FF:DialogServiceViews.IsRegistered="True"  
        
        Title="R.Brenckman - ITE217 - Week 4 - Singletons" Height="450" Width="800" ResizeMode="CanResize" SizeToContent="WidthAndHeight"
        mc:Ignorable="d"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        <TextBlock 
                Grid.Row="0" Grid.ColumnSpan="10"
                Text="This is the Main Window. Press one of the following buttons : "/>

        <Button
            Grid.Row="1" Grid.Column="1"
            Content="Show Popup One"
            ToolTip="Displays a Non-Modal popup"
            Command="{Binding ShowPopupOneCommand}"
            />

        <Button
            Grid.Row="1" Grid.Column="2"
            Content="Show Popup Two"
            ToolTip="Displays a Modal popup"
            Command="{Binding ShowPopupTwoCommand}"
            />
    </Grid>
</Window>
